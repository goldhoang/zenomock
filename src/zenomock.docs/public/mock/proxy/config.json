{
  "endpoint": "GET /api/v1/proxy/config",
  "enabled": true,
  "upstreamBaseUrl": "https://httpbin.org",
  "allowedHosts": ["httpbin.org"],
  "timeoutSeconds": 10,
  "note": "Showroom snapshot — live proxy requires the local engine"
}
